// The classic "Hello World" -- but note this is plain Scala, NOT hardware.
// It runs on the JVM and simply prints a string. We use it only to prove the
// toolchain (sbt + Scala) works before we describe any hardware.
object HelloScala extends App {
  println("Hello Chisel World!")
}
